@model IEnumerable<WebAppCK.Models.table_catagory>

@foreach (var i in Model)
{
    <div class="spec">
        <h3><a href="@ViewBag.meta/@i.meta/">@i.name</a></h3>
        <div class="ser-t">
            <b></b>
            <span><i></i></span>
            <b class="line"></b>
        </div>
    </div>
    <div class="con-w3l">
       
            @Html.Action("getProduct", "Default", new { id = @i.id, metatitle = @ViewBag.metatitle })
       
     </div>
}
